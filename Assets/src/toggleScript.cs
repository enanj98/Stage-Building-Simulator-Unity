using UnityEngine;

public class DiscoLightState : MonoBehaviour
{
    public bool randomColorToggleState = false; // Stores the toggle state
    public Color lastSelectedColor = Color.white; // Stores the last selected color, initialized to white
}